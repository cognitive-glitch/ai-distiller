[package]
name = "lang-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
distiller-core = { path = "../distiller-core" }
tree-sitter = "0.25"
tree-sitter-rust = "0.24"
parking_lot = "0.12"

[dev-dependencies]
insta = { version = "1.34", features = ["yaml"] }
