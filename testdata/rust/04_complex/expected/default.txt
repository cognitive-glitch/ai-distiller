<file path="/home/janreges/ai-distiller/testdata/rust/04_complex/source.rs">
use std::sync::{Arc, Mutex};
use std::ffi::{c_char, CStr};
use std::future::Future;
use std::pin::Pin;

struct Config;

struct AnalysisService;

mod impl AnalysisService {

    fn new() -> Self

    async fn validate_code(&self: , code: &str) -> Result<bool, &'static str>
}

mod AsyncProcessor;

mod impl AsyncProcessor for AnalysisService;

mod impl FFIData {

    fn new_integer(value: i64) -> Self

    fn get_integer(&self: ) -> i64
}
</file>
