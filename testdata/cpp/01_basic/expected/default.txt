<file path="source.cpp">
#include "iostream"
#include "string"
#include "vector"
T max(T& a, T& b):
    {
            return (a > b) ? a : b;
        }

T min(T& a, T& b):
    {
            return (a < b) ? a : b;
        }

int main()
</file>
