<file path="/home/janreges/ai-distiller/testdata/typescript/04_complex/source.ts">
type PluginSettings = {
  [K in 'timeout' | 'retries']?: K extends 'timeout' ? number : number;
}

interface IPlugin:
    property name: string
    method execute(data: any): void

class PluginManager:
    function constructor(settings: PluginSettings)
        // implementation
    function registerPlugin(plugin: IPlugin) -> void
        // implementation
    function runAll(data: any) -> void
        // implementation
</file>
