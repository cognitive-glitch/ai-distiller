<file path="source.swift">
import Foundation
import Combine

class Clamped:
    var wrappedValue: Value
    var projectedValue: AnyPublisher<Value, Never>

class ValidationResult:

class ConfigurationBuilder:
    buildBlock(_: ValidationResult) -> [ValidationResult]
    buildExpression(_: String) -> ValidationResult
    buildEither(first: [ValidationResult]) -> [ValidationResult]
    buildEither(second: [ValidationResult]) -> [ValidationResult]

protocol Describable:

class SettingsProxy:

class UserSettings:
    asProxy() -> SettingsProxy
</file>
