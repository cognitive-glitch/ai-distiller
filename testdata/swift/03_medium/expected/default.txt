<file path="source.swift">
import Foundation

class Stack:
    push(_: Element)
    peek() -> Element?
    pop() -> Element?

class :

class MathError:

protocol Cacheable:

class ExpiringCache:
    insert(_: Value, for: Key)
    value(for: Key) -> Value?
    contains(key: Key) -> Bool
</file>
